#include<stdio.h>
#include<stdlib.h>
#include<math.h>


void main14() {

	int n = 1;
	printf("%lf\n",pow(10, 2));			//计算10的2次方

	char* str = "1234mikelljlk";
	char buf[1024] = { 0 };
	sscanf(str, "%*s", buf);
	printf("%s\n", buf);



}